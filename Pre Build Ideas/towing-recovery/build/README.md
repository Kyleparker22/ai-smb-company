# Hook OS — build

Towing & recovery. Port **8854** (`prebuild-hook-os`).

```
python3 seed.py         # synthetic Ironline Towing
python3 test_hook_os.py # the suite
python3 server.py       # 127.0.0.1:8854
```

## The load-bearing refusals
- **The rate-card clamp.** Every invoice computes from the filed card by construction — a
  requested number above it is refused with both numbers shown.
- **Damage answers come from the hookup photo set or not at all** — never the dispatcher's
  confidence.
- **The lien calendar alerts; counsel files.** Auction/sale is a human decision after the legal
  steps; the system can express neither.
- **Storage runs on recorded stamps** — impound to release, backdating structurally refused.
- **Rotation calls read first** and record at R2; that clock is measured in minutes.

## Honesty rules (from `_kit`)
Costly eval label `rotation`. Recovered-this-week counted (rotation calls, releases, storage
billed). ROI typed; the rate-card defense is a scenario. Default rate card and lien rules name
themselves replaceable. Synthetic only; nothing is sent.
