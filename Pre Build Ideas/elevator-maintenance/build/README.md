# Cab OS — build

Elevator & escalator maintenance. Port **8860** (`prebuild-cab-os`).

```
python3 seed.py        # synthetic Vertex Elevator Service
python3 test_cab_os.py # the suite
python3 server.py      # 127.0.0.1:8860
```

## The load-bearing refusals
- **The desk script never allows self-evacuation** — the words cannot be produced; climbing out
  of a stalled car is the fatality mechanism.
- **A red-tagged unit returns only on the clearing mechanic's recorded sign-off** — software can
  never reactivate one.
- **A test with no record reads UNKNOWN**, never compliant.
- **Nothing is billable off contract silence** — the clause is cited, or the verdict is
  ambiguous and a human decides.

## Honesty rules (from `_kit`)
Costly eval label `entrapment`. Callback rate counted with a floor of 30. Recovered-this-week
counted. ROI typed; the entrapment log is a scenario. Synthetic only; nothing is sent.
