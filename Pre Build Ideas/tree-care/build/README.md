# Canopy OS — build

Tree care. Port **8852** (`prebuild-canopy-os` in `.claude/launch.json`).

```
python3 seed.py           # synthetic Hartwood Tree Company
python3 test_canopy_os.py # the suite
python3 server.py         # 127.0.0.1:8852
```

## The load-bearing refusals
- **Neither "safe" nor "hazardous" leaves software.** The hazard-ask reply books the arborist and
  judges nothing — tested for both verdict words.
- **No utility clearance, no crew.** A power-line-flagged job cannot be scheduled without the
  recorded clearance reference; energized lines are the fatality mechanism of this trade.
- **Storm emergencies read first** and go to a human coordinator with a 911-first ack.
- **No felling promises** in any outward copy.

## Honesty rules (from `_kit`)
Costly eval label is `emergency`, reported alone. Estimate ladder: 3 touches, 6-day cooldown,
silence is an answer. Recovered-this-week counted (estimates won, PHC renewed, callbacks). ROI
typed and labelled a model; the storm hour is a scenario. Synthetic only; nothing is sent.
