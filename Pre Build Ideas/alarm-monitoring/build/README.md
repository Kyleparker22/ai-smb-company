# Central OS — build

Alarm monitoring & installation. Port **8864** (`prebuild-central-os`).

```
python3 seed.py            # synthetic Beacon Alarm & Monitoring
python3 test_central_os.py # the suite
python3 server.py          # 127.0.0.1:8864
```

## The load-bearing refusals
- **Test mode never comes from a message.** The request is refused, preserved verbatim with the
  sender, and becomes a verified-callback task — the burglar's first move, structurally shut.
- **A passcode in a thread is never accepted, compared, or confirmed** — the callback rule holds,
  and the customer copy teaches it.
- **A fire dispatch is never cancelled by this system.** No exceptions exist. Burglary cancels
  are human decisions after a verified recorded callback (R1, never promoted).
- **Permits & fines** computed against each city's recorded ordinance table as DATE ALERTS.

## Honesty rules (from `_kit`)
Costly eval label `test_mode_request`. Recovered-this-week counted (SE refusals, callbacks,
renewals). ROI typed; the refusal log is a scenario. Synthetic only; nothing is sent.
