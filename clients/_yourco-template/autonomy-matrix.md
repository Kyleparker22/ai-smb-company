# Autonomy Matrix — <CLIENT> (engagement instance)

> Filled in during discovery. Standard + rung definitions: `processes/autonomy-matrix.md`. The client dials their appetite per action; **"zero human time" = every action at the highest rung its eval evidence + reversibility support.** Owners: Kimi (runs at current rung) + Kolby (advancement) + the client (sets appetite, holds the kill switch).

## This engagement's controls (the no-human stack)
- **Kill switch** held by: `<client contact>`
- **Reversibility window** (R2 actions): `<N> min`
- **Spend cap** (money actions): `$<X>/txn · $<Y>/day` · **Rate caps:** `<…>`
- **Exception escalation** → `<client contact / channel>`
- **Audit log:** `<where>` · **Eval owner:** Kolby · **Compliance:** Rafi · **SLA / liability:** `<ref — Ray>`

## The grid (per capability)
| Capability | Action class | Current rung | Ceiling | Advance when (eval evidence) | Notes |
|---|---|---|---|---|---|
| _e.g._ Answer + triage inbound | read + draft | R1 → | R3 | 50 drafts · ≥98% approved · 0 incidents | |
| _e.g._ Book appointments | schedule (own cal) | R2 | R3 | 2 wks · 0 misbookings | reversible N min |
| _e.g._ Quote follow-ups | email known contact | R1 | R3 | 30 sends · ≥99% on-brand · 0 complaints | |
| _e.g._ Message the client's customers | email customers | R1 | R2\* | — | client = **sender-of-record** |
| _e.g._ Invoicing / AR | money | R1 | R2 | — | hard cap `$<X>`; never unattended w/o a standing guardrail |
| _e.g._ Anything destructive | irreversible | R1 | R1 | — | stays gated by design |

\* Capped ceiling — never unattended R3 without a named exception + counsel.

## Advancement log (Kolby)
| Date | Capability | R→R | Evidence | Approved by |
|---|---|---|---|---|
| | | | | |

> **Honest start:** unproven + high-stakes actions begin at **R1** and climb on evidence — the gated start is what lets us safely reach zero-touch, not a delay. A client wanting "the AI runs everything" gets exactly that, as fast as the evals earn it, with the kill switch in their hand the whole way.
