> ⚠️ **EXAMPLE OUTPUT — not yours.** This is one run of this loop from the company this
> template was extracted from, kept so you can see the shape of what the loop produces.
> The dates, numbers, and findings describe **someone else's business**. Delete this file
> the first time your own loop writes a real one.

# Customer Health — 2026-08-19

> **Cadence check** (per `learnings/ops/2026-07-28_loop-liveness-blindspot.md`): prior artifact **2026-08-12**, today **2026-08-19** — exactly one weekly interval. **No gap.** Three consecutive on-schedule runs.

> **Prediction scorecard** (per `learnings/ops/2026-07-22_predict-score-adjust.md`). Last run's prediction, made 2026-08-12, to be scored today:
> *"The 1-pager will still be unsigned and the walkthrough will have happened."*
> - **Part A — still unsigned: TRUE.** `clients/sample-client/_README.md` line 2 unchanged — *"still at Proposal, not signed — pre-revenue."* No signed agreement in `attachments/`; `cost.md` still books discovery/CAC.
> - **Part B — walkthrough happened: FALSE.** Calendar 08-12→08-23 = **0 events**. `_README` still lists the walkthrough as an open item (line 43). No client-side comms this week.
> **Net: half right, and the reality is worse than the miss implies.** The escalation the prior run tied to "both hold" was really about *the gap between delivery and paper widening*. That gap widened **more** than predicted: not only did the close event slip a second week, but live Aspire production data began flowing pre-signature. Delivery accelerated while both the paper and the close event stalled.

## Summary
- Green: 0
- Yellow: 0
- **Red: 1 — Sample Client** (scored by exception; escalated from yellow — see below)

**On the "pre-revenue, nothing to monitor" framing:** by the CRM stage flag this is still a 0-`live`/0-`expansion` week (23 deals: 20 `pre-convo`, 2 `discovery`, 1 `demo-proposal`), so SOP step 2 would read "no active engagements" for the eighth run running. It is not one — same finding as the last two runs. Sample Client is an active delivery engagement (this week's git log: VISIT MODE, the render pipeline, and a **live Aspire schedule pull of 1,224 tickets** all shipped; integration board ALL GREEN 2026-08-18). Nothing here is invented — every signal below traces to a git commit, a client-folder file, the calendar, or the inbox. I scored it by evidence, not by the enum, per `learnings/ops/2026-08-07_absence-is-invisible-to-this-os.md`.

## Per engagement

### Sample Client — **red** (escalated from yellow 2026-08-12)

Red here is a **structural / compliance** red, not a relationship one. The client is engaged and not unhappy; the problem is entirely on yourco's side of the line, and it is red because an **irreversible gate has been crossed**, not merely approached — which is the difference from last week's yellow.

**Evidence (all this week; verified against files/calendar/inbox, not a checkbox — `learnings/ops/2026-07-07_checkbox-is-not-clearing-evidence.md`):**
- **Live client data is now flowing pre-signature.** `_README.md` line 40 (updated 2026-08-18): integration board **ALL GREEN** — Aspire live (dedicated read-only API record), HubSpot live. Commit `2c8a140`: a live `/api/aspire/schedule-pull` returning **1,224 scheduled tickets**, cached to shared. That is real Sample Client production data ingested into yourco's platform.
- **The engagement is still unsigned.** `_README.md` line 2 — *"still at Proposal, not signed — pre-revenue."* No signed 1-pager in `attachments/`.
- **The gate this crosses is named by the client folder itself.** `_README.md` line 35 (Ray's role): *"counsel gate on the signed 1-page agreement **before any live-data/production wiring**."* Line 43: *"signed 1-pager opens Ray's gate for live data flows (Aspire actuals pull, HubSpot stage sync, write-back scopes)."* Reading 1,224 live tickets is live data; on the plain reading of line 35 the gate has been crossed. There is an internal interpretation that a **read-only** schedule pull is acceptable pre-signature and only *actuals-pull + write-back* is post-signature — but **no one with authority has ruled on that split.** The unruled ambiguity is itself the risk.
- **No counsel is engaged to clear it.** `processes/counsel-gates.md` — "Counsel firm / contact: *none engaged yet*." So the gate cannot be cleared even if the Founder wanted to sign today; the paper does not exist and neither does the reviewer.
- **The walkthrough — which is the close event — slipped a second week.** Calendar 08-12→08-23 has **0 events**. `_README` line 43 still carries it as open. The week the Founder named (wk of 08-10) is now nine days past with nothing booked.
- **Client silent 9 days.** Last client-side comms = the 08-10 credential exchange. Gmail 14-day sweep for sampleclient.example.com / Client Owner / Charlene / Aspire returned **only yourco's internal Monday Briefing** — zero client mail. Crosses the 7-day silence trigger (yellow floor on its own).
- **Health surface still absent — 7th consecutive flag.** No `clients/sample-client/weekly/`, no `03_eval.md` (glob confirmed empty). The engagement it was meant to measure is now shipping live-data features with nothing scoring them.

**Why red, not another yellow:** last week the risk was *in flight* ("wiring in motion" — the run's own words) and the recommendation was "do not wire live client data until the 1-pager is signed — cheap now, expensive later." This week the live data has flowed. The cheap-now item became the expensive-later side. An unsigned, un-counseled engagement is now ingesting a client's production job data. That is the line the prior run drew, crossed.

**Recommended action — routed to the Founder, in order:**
1. **Rule the read-only-vs-signature question, explicitly.** the Founder (with Ray) decides whether the read-only Aspire schedule pull is within or across the line 35 gate, and **records the ruling in `_README.md` + `counsel-gates.md`** so it is not re-litigated. If across: pause the pull until signed. If within: write down why, so the gate text stops reading as violated. Do not leave it unruled a second week.
2. **Book the walkthrough now.** It has slipped twice and it is the close event ("close the $1,000/mo start on it"). Every week it slips, paper and revenue slip with it, and the live-data gap widens.
3. **Get the 1-pager signed — it is the single lever that resolves items 1 and 2.** Signature opens Ray's gate for the live flows already half-built, converts CAC spend to revenue, and removes the compliance red at the root.
4. **Credential + identity hygiene (carried from last run, partially addressed).** Secrets are now in gitignored `platform/.env` (`_README` line 40) — good, that closes the plaintext-in-inbox exposure `learnings/ops/2026-08-02` named. Still open: request a **named** Aspire user for the Founder vs. the shared login, and confirm client correspondence is on `founder@yourco.example.com`, not the personal Gmail (CLAUDE.md flags that separation as having leaked three times).
5. **Scaffold `weekly/` + `03_eval.md` this week.** No longer optional — live features are shipping unmeasured, and the Aspire-actuals quote-accuracy loop (the moat demo, `_README` line 32) has nowhere to write.

### Prospect A — not scored (no delivery activity)
`discovery`, lastTouch **2026-06-30 — 50 days stale**. POC scope (due 7/07) ~6 weeks past with nothing logged. No Gmail/Slack signal this week. Sales-loop item, carried for continuity.

### Sample Realty — not scored (pre-engagement)
`discovery`, lastTouch **2026-08-07**. Give-first stack delivered; no client-side delivery obligation yet, so nothing to health-score. Carried for continuity.

## New predictions (score on 2026-08-26)
1. The 1-pager will still be unsigned on 2026-08-26 — **confidence 65%.** (It has been unsigned since June; nothing this week moved it.)
2. The walkthrough will have happened by 2026-08-26 — **confidence 45%.** (Slipped twice; it is the close event, so pressure to hold it is rising — genuinely uncertain.)
3. No `weekly/` or `03_eval.md` will exist in `clients/sample-client/` by 2026-08-26 — **confidence 60%.** (Flagged 7 runs, still not built; build focus is on client-facing platform features, not the internal health surface.)

## Connector check (this run)
- **CRM/folders:** read `clients/_pipeline.md` (reconciled to `crm/data.json` 2026-08-17 — 23 deals, 0 `live`/`expansion`); read `clients/sample-client/_README.md` (contract + integration state) and confirmed no `weekly/`, no `03_eval.md` via glob; checked `processes/counsel-gates.md` for the counsel-engagement + gate state; used this week's git log (`2c8a140`, `4e3d087`, `fbdcd20`) as the delivery-activity signal.
- **Gmail:** searched sampleclient.example.com / Sample Client / Client Owner / Client Owner / Charlene / Aspire at 7d then 14d → the only hit is yourco's internal Monday Briefing (08-17). **Zero client-side mail in 14 days.** No message opened beyond search metadata; nothing sent, no draft.
- **Calendar:** listed `primary` 08-12→08-23 → **0 events**. This is what confirmed the walkthrough did not happen (prediction Part B).
- **Slack:** not separately searched this run — the material signals resolved from git + calendar + inbox, and last run's Slack sweep found only David's automated CRM-hygiene digests, no human client discussion. Noted as a gap rather than claimed as covered.
- **Writes:** this artifact + one Slack post to `#yourco-kortney` (per the run's instruction; a red present would trigger the SOP post regardless). No CRM edit, no client-folder change, no email sent.

## What I'd do differently next run
(Empty — for the Founder to fill)

## What worked this run
- **The predict-score-adjust loop paid off on its first scoring.** Last run committed a falsifiable prediction; this run scored it in under a minute against the calendar and the README, and the *miss* (walkthrough didn't happen) was more informative than a hit would have been — it revealed delivery outrunning both paper and the close event. This is the compounding calibration track the learning promised; keep making 1–3 scoreable predictions every run.
- **The escalation had a pre-committed trigger.** Yellow→red wasn't a vibe — the prior run wrote down the exact condition ("live data before signature = the expensive side") and this run checked it against a git commit. A status change with a receipt is eval evidence, not opinion.

## Learnings applied this run
- `learnings/ops/2026-08-07_absence-is-invisible-to-this-os.md` — organizing idea: refused to let "0 `live` deals" render as health; scored the engagement the CRM enum can't see, using freshness/gate checks rather than failure checks.
- `learnings/ops/2026-07-22_predict-score-adjust.md` — scored last run's prediction at the top; committed three new falsifiable ones for 2026-08-26.
- `learnings/ops/2026-07-28_loop-liveness-blindspot.md` — cadence check front-loaded and reported on pass (08-12→08-19, no gap).
- `learnings/ops/2026-07-07_checkbox-is-not-clearing-evidence.md` — "unsigned" verified against `_README` prose + `attachments/` + `cost.md` coding; the gate checked in `counsel-gates.md` directly; the live-data claim traced to commit `2c8a140`, not a status line.
- `learnings/ops/2026-08-02_synced-workspace-is-not-a-privacy-boundary.md` — confirmed the prior plaintext-credential exposure is now closed (`platform/.env`, gitignored) and folded the residual named-user/identity items into recommendation 4.
- `learnings/ops/2026-08-09_inference-only-where-judgment-is-needed.md` — dates/counts/staleness read directly from files/calendar; inference spent only on the judgment call (is the gate crossed, is this red).
- `learnings/delivery/2026-07-05_client-build-stack-defaults.md` — read; the Aspire/HubSpot adapters are the client's incumbent systems, so the defaults apply to what yourco builds around them. Not otherwise actionable this run.

## Tooling note (honest)
Bash is denied on this runtime (approval gate), so the contract's Step-0 trigger engine (`runtime/learning_triggers.py`) and the anti-library check (`runtime/rejections.py`) could not be run. I satisfied the contract's floor manually — the domain+recency read of `learnings/delivery/` + `learnings/ops/` via Glob + Read (applied entries listed above). On the anti-library: this run proposes no new idea/tool/channel/direction — recommendations 1–5 are applications of gates and health-surface obligations already in the client folder and `counsel-gates.md`, so there is nothing novel to check against `rejections/`.
