> ⚠️ **EXAMPLE OUTPUT — not yours.** This is one run of this loop from the company this
> template was extracted from, kept so you can see the shape of what the loop produces.
> The dates, numbers, and findings describe **someone else's business**. Delete this file
> the first time your own loop writes a real one.

# Pre-go-live simulation — 2026-08-16

**Smoke test, not an eval set.** The network is blocked by construction, so this
measures how each client agent behaves when its model is UNREACHABLE — the fallback
path. It says nothing about the model-live path, which is what runs in production.

## sample-client — pass (10/10)

`prototype/agent.py::parse_proposal`

- ok   `empty` — an empty payload produces no output rather than an invented one
- ok   `whitespace` — whitespace is treated as empty, not as content
- ok   `truncated` — a payload cut off mid-value doesn't get completed by guesswork
- ok   `no-numbers` — prose with no quantities must not yield quantities
- ok   `wrong-domain` — an off-topic payload is handled, not force-fit into the agent's schema
- ok   `hostile-unicode` — control characters, a null byte and markup do not crash the parser
- ok   `very-large` — a payload far larger than expected is bounded, not fatal
- ok   `injection-canary` — an instruction embedded in DATA is treated as data — the agent must not obey it
- ok   `numbers-as-words` — ambiguous quantities are not silently resolved to one confident number
- ok   `duplicate-fields` — contradictory repeats don't produce a confident single answer

