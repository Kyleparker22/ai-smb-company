> ⚠️ **EXAMPLE — not yours.** An artifact from the source company, restored because other pages
> cite it and the reasoning would otherwise dead-end. It describes **someone else's business**.

# Finance Pulse — 2026-06-07

## Watchdogs fired
**OPERATIONAL ALERT — Google Workspace payment declined (yourco.com).** On Jun 1, 2026, the Mastercard ending 9281 was declined for the Google Workspace subscription; reason given by the bank: "insufficient funds." A new monthly invoice (#5580728454) issued Jun 2 will auto-charge the same card and will likely fail again. This is core infrastructure — it hosts founder@yourco.example.com and is the mechanism for the named digital-employee emails in client tenants. If it lapses, delivery capability lapses. Fix the payment method today.

Note: this isn't one of the SOP's numbered triggers (those are revenue/margin/runway based, none of which can be evaluated while ledgers are missing — see gaps). Flagging it as an operational watchdog because an "insufficient funds" decline on the primary card is both a continuity risk and a possible cash signal.

## Current state
- Cash: unknown — `runway.md` does not exist (set at monthly close; the Founder to supply)
- MRR: $0 — pre-revenue, no active engagements
- Estimated burn: unable to compute — `expenses.md` does not exist. Only confirmed recurring cost this period is Google Workspace Business Starter (invoice #5580728454, amount in attached PDF, not yet logged). Apollo subscription also appears active per vendor email.
- Runway: not computable without a cash figure

## Logging gaps to fix this week
- **All four ledger files are missing**: `revenue.md`, `expenses.md`, `token_spend.md`, `runway.md`. `finance/README.md` and `monthly_close.md` exist, but the actual ledgers do not. This is the top gap — the loop cannot compute state until these exist.
- Google Workspace invoice #5580728454 (issued 2026-06-02) is unlogged → belongs in `expenses.md`, category `tooling`. Amount is in the PDF attachment.
- Apollo (apollo.io) appears to be an active paid tool → confirm and log under `tooling` once `expenses.md` exists.
- This is the first finance-pulse run; no prior artifact existed in `loops/finance/`.

## Per-engagement margin
No active engagements (pre-revenue).

## What to do this week
1. **Fix the Google Workspace payment today.** Update the card / resolve the declined Mastercard ending 9281 in admin.google.com → Billing, and clear the outstanding balance before service interrupts.
2. **Create the four ledger files** in `/finance/` (`revenue.md`, `expenses.md`, `token_spend.md`, `runway.md`) using the conventions in `finance/README.md`. Even a stub for each unblocks every future run.
3. **Log the Google Workspace invoice** (#5580728454, 2026-06-02, `tooling`) and confirm/log Apollo as the first `expenses.md` entries.
4. **Set cash on hand in `runway.md`** so burn and runway can be computed next Monday. The declined-card "insufficient funds" message makes a real cash figure urgent.

## What I'd do differently next run
(Empty — for the Founder to fill before next Monday)
