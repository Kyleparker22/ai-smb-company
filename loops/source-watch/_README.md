# loops/source-watch/ — weekly named-source steal sweep (Brett)

One dated artifact per run (`YYYY-MM-DD.md`): new items from the active roster in `processes/loops/source-watch.md`, each with a tool-triage verdict (adopt / steal / trigger-gate / skip). `state.json` holds per-source seen-item IDs — runtime-owned, don't hand-edit. Proposed steals wait for the Founder's approval before anything is absorbed into `decisions/` or agent docs.
