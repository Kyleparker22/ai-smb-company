> ⚠️ **EXAMPLE OUTPUT — not yours.** This is one run of this loop from the company this
> template was extracted from, kept so you can see the shape of what the loop produces.
> The dates, numbers, and findings describe **someone else's business**. Delete this file
> the first time your own loop writes a real one.

# Open Loops — 2026-08-21 (Fri)

> Owner: Jim (chief of staff). **Fifth consecutive timer-fired run since the runtime resumed 08-16** (Mon 08-17 reconciliation → Tue 08-18 → Wed 08-19 → Thu 08-20 → today). Runtime confirmed **live** this run — today's git log shows `loop:source-watch`, `loop:content`, `loop:inbox-triage` all fired 2026-08-21, and inbox-triage's own 08-21 artifact is on disk. A normal quiet +1 daily tick (08-20 → 08-21), not a paused-then-catch-up. `crm/data.json` `meta.updated` reads **2026-08-19** — **unchanged since yesterday's run**; a field-level check of the four in-motion deals (d11/d18/d29/d30) finds every `stage`/`stageSince`/`nextDate`/`lastTouch` identical to yesterday. All four actionable the Founder-send drafts re-verified present in `in:drafts` by ID today; no counsel/launch gate moved; watchdog/eval-review artifacts unchanged (08-16 / 07-12). So **nothing cleared**, every carried item ages +1, and **Sample Client (#9) flips 🟡→🔴 today exactly as flagged yesterday.** Honest read: a flat board with one predicted age-flip (expected pre-launch — SOP §Pre-scale: grade the honesty of the ages, not the churn).
>
> **Completion contract (fixed before working):** Done-state = this dated artifact in SOP format + one Slack post to `#yourco-jim`. Verification = every age computed `2026-08-21 − origin`; every draft re-confirmed present in `in:drafts` by ID today; deals reconciled against substantive CRM fields (`stage`/`stageSince`/`nextDate`/`lastTouch`), not `done` flags; §6b swept field-by-field for `referredDate`. Don't-touch = report/remind only; no send, no delete, no stage move, no decision-file edit. Stop = artifact + post written, or the shortfall named.
>
> **Learnings applied this run (Step 0 — by hand; `learning_triggers.py`/`rejections.py` require Bash, which is gate-denied this session — no Bash tool present, per `learnings/ops/2026-08-20_headless-gate-blocks-in-session-instrumentation.md`; Glob'd `learnings/ops/` (22 entries) and read the relevant ones directly per `2026-08-19_anti-library-hand-check-needs-glob.md`):**
> - `learnings/ops/2026-07-07_checkbox-is-not-clearing-evidence.md` — applied 2×: (a) `t_hank` `done:true` re-checked against its fields — c18 still literally `"— Sample Contact's business (the Founder to fill)"` and p15 email+phone blank, verified directly this run (CRM `meta.updated` static since 08-19 → flag unchanged), **9th consecutive run** the checkbox contradicts the fields, so not cleared; (b) all four actionable drafts claimed "unsent" verified present in `in:drafts` by ID **today** (10DLC ×2, Free-trial, ForesightIQ), not assumed from yesterday.
> - `learnings/ops/2026-07-28_loop-liveness-blindspot.md` — cadence check FIRST: confirmed a genuine +1 daily tick (08-20 → 08-21) via today's git loop-commits + the fresh inbox-triage 08-21 artifact; ages are `today − origin`, not "+1 per calendar day since birth."
> - `learnings/ops/2026-07-06_cross-session-drift.md` — reconciled against substantive deal fields; did not hand-edit the CRM; treated the static `meta.updated` skeptically and re-checked the in-motion deal fields rather than assuming.
> - `learnings/ops/2026-08-20_headless-gate-blocks-in-session-instrumentation.md` + `2026-08-19_anti-library-hand-check-needs-glob.md` — used the documented Glob substitution for the gate-denied Step 0 scripts instead of skipping Step 0.
>
> **Sources actually read this run:** prior artifact `2026-08-20.md`; today's `loops/inbox-triage/2026-08-21.md`; `crm/data.json` (companies + full contact/connector/partner roster + deals incl. d11/d18/d29/d30 + §6b `referredDate` sweep; `meta.updated` 2026-08-19); Gmail `in:drafts` (23 shown, 4 actionable checked by ID + date); `loops/_watchdog/2026-08-16.md` (latest — unchanged); latest `loops/eval-review/2026-07-12.md` (paused since — no newer); `runtime/proposed-holds.md`.
>
> **Anti-library:** no new idea/tool/channel is *proposed* this run — the ">21d → act/delegate/park" lines are the SOP's own decision-forcing mechanic, not new proposals — so the `rejections.py` check is N/A here (and unrunnable this session regardless).

## Cleared since last run — none

Nothing cleared with hard evidence. CRM substantively unchanged (`meta.updated` static at 08-19); all four actionable drafts still present in `in:drafts`; no counsel/launch gate moved. Every one of the 15 prior queue items is accounted for below (all carried +1). **No item vanished without evidence.**

## The queue — waiting on a human, ranked stakes × age, against **2026-08-21**

*(🔴 = >7 days; 🟡 ≤7d. Rank orders by stakes × age — the color is the age-flag; the order carries the stakes.)*

| # | Item | Whose action | Waiting since | Age | Next step |
|---|---|---|---|---|---|
| 1 🔴 | **Warm-network outreach still not started.** ~20 warm prospects in pre-convo; d19–d28 all `lastTouch 2026-06-16`, contacts `gone-cold`; d8–d16 never touched. The 2026-07-04 audit's core commercial gap, **unmoved across every run and through the pause.** Copy for Joey/Tucker/Brigitte was written by the 07-06/07-13 sales loops — only *send* remains. | the Founder | 2026-06-13 | **69d** | Send the 3 ready warm texts, or delegate sequencing to Reilly. **>21d, 17th carry — a decision made by drift; act or formally delegate.** ⚠ **5th consecutive-run no-movement → leads the Slack post in bold** (SOP watchdog trigger). |
| 2 🔴 | **Live deal — Sample Client (d11) now 8d past its next action, and 🔴 as flagged.** `stage demo-proposal`, `nextDate 2026-08-13` (**8d past**), `lastTouch 2026-08-06`. `td_seed1` (08-13, `done:false`): "Send Client Owner the Design Studio walkthrough link and ask for a decision date." Deal-agent flagged "no draft attached at proposal" ×3. **Highest-value, closest-to-close deal ($12k) — leads the revenue block; crossed 🟡→🔴 today exactly as yesterday predicted.** | the Founder | 2026-08-13 | 8d | Ship/confirm platform v1 for Colton, send Client Owner the walkthrough link, and **ask for a decision date** (td_seed1). |
| 3 🔴 | **Legal suite → counsel (t_legal), and the OA needs the Founder BEFORE counsel.** `counsel-gates.md` still records **no counsel engaged** — one engagement moves 14 gates. Gate #14 (the 50/35/15 OA) regressed 🟠→🔴 on 08-10: three blocking *the Founder* decisions first — **D10** voting threshold, **D11** deadlock end-game, **D12** whether Mike's obligations mirror Partner B's. `td_seed2` ("OA blocks to counsel", `done:false`) stays premature until D10–D12 are answered. **Wrinkle:** Sample Contact is still marked connector→"prospective PARTNER" (08-18) against a **fully-allocated** 50/35/15 — the cap table can't seat a 4th without a split change; folds into this decision. | the Founder | 2026-06-18 | **64d** | (a) Answer D10/D11/D12; (b) engage counsel + send the legal-suite package. Highest-leverage single move in the OS. |
| 4 🔴 | **$0 cash + burn triage unexecuted.** `finance/runway.md` (08-05): **$0 cash**, 0.0-mo runway vs ~$614/mo fixed. Charles named ~$252/mo of cuts (dup Instantly $194 · Descript $35 · Granola→free $14 · Plausible $9) → ~$362/mo floor. **No evidence any cut executed.** Every unexecuted day is borrowed from the Founder personally. | the Founder | 2026-08-05 | 16d | Execute the four cancellations (or explicitly decline each). ~20 min, one session. |
| 5 🔴 | **Prospect A (d29) — 7d past its next action, partnership still unpapered.** `stage discovery`, `nextDate 2026-08-14` (**7d past**), `lastTouch 2026-06-30` (**52d**). Sample Product still built at depth with **no terms** — counsel gate #11 (*"not counsel-heavy, a one-pager closes it; do first"*). Deal-agent flagged STALE + "no draft attached" ×2 (08-11). | the Founder | 2026-06-30 | **52d** | Make the 08-14 next-step real (confirm/rebook) + paper the one-pager: IP, split, who pays data costs. |
| 6 🔴 | **Runtime durable billing/liveness fix.** The 08-04→08-16 outage was a deliberate pause, not a death — but the *cause* (the 07-16→08-04 credit + git-rebase stretch) is unfixed: Anthropic auto-reload on a funded card + low-balance alert + an **off-box** dead-man's-switch, recommended across 3+ occurrences. Collides with #4 (auto-reload on an unfunded card just relocates the failure). | the Founder / platform | 2026-06-18 | **64d** | Decide: fund API + auto-reload + wire healthchecks.io/UptimeRobot, **or** formally park the runtime until revenue. Also confirm the 07-30→08-04 git-rebase conflicts are clean (watchdog flag). |
| 7 🔴 | **10DLC registration reply — two drafts, still unsent.** Verified in `in:drafts` today: `19eb64b6` (06-11) + `19eeef0b` (06-22). Blocks A2P/SMS entirely. | the Founder | 2026-06-11 | **71d** | Fill the `[FOUNDER …]` bracket, send. Single action clears a whole capability. **>21d — act or park the SMS channel.** |
| 8 🔴 | **2FA sweep (t_2fa).** Security hygiene across all accounts — pointed, given account-lifecycle events have driven the runtime dark/paused repeatedly. | the Founder | 2026-06-16 | **66d** | Run the sweep. **>21d.** |
| 9 🔴 | **Sample Contact (d18 / t_hank) — the checkbox still contradicts every field, 9th run.** `t_hank` remains `done:true`; c18 still literally `"— Sample Contact's business (the Founder to fill)"`, p15 email+phone blank, d18 `nextDate 2026-06-19` (**63d past**). | the Founder | 2026-06-19 | **63d** | Fill business name + contact and book the audit, **or** record the decision to deprioritize and clear the flag honestly. **>21d.** |
| 10 🔴 | **Sample Realty sit-down (t_parker_sitdown / d30).** `stage discovery`, `lastTouch 2026-08-07`; large give-first stack delivered (site, tours, PM packet+console, 'year reconciled'). Both the task `due` **and** the deal `nextDate` are **empty** — nothing will ever nag it but this loop. The give-first is spent; the ask converts it. | the Founder | 2026-08-04 | 17d | Put a date on the sit-down with Kimi. |
| 11 🔴 | **Calendar-create gate-scope confirmation** — confirm `calendar-create` sits in the `~/.claude/settings.json` allow-set deliberately, not by inheritance (eval-review 06-28). | the Founder / platform | 2026-06-28 | **54d** | Confirm intent, note the decision. **>21d — decide or drop.** |
| 12 🔴 | **"Free trial ending" reply draft** (`19f1d4a3`, 07-01) — verified still in drafts, unsent. Service unnamed; renew/cancel undecided. **Overlaps #4** — likely a cancel. | the Founder | 2026-07-01 | **51d** | Decide keep vs cancel; send or discard. **>21d.** |
| 13 🔴 | **HighLevel account deletion in 30 days** — notice received 08-07 (deletes ~09-06, **~16d out**). Was an off-books charge. | the Founder | 2026-08-07 | 14d | Let it delete (default, saves money) or export first. Decide once. |
| 14 🟡 | **ForesightIQ demo-request reply — draft `1a00f580…`, still unsent.** Verified in `in:drafts` today (created 08-17). Inbox-triage 08-21 carries it as a "still your call" fading item (day 5) — the Founder-as-buyer evaluating a vendor tool, not a prospect. Surfaced here only so a the Founder-send draft doesn't age silently. | the Founder | 2026-08-17 | 4d | Send the draft + grab a slot on Nate's link, or tell inbox-triage it's dead and stop carrying it. ⚠ Any external send is OtherVenture-gated. |
| 15 🟡 | **Sample Contact (d32) — new-vertical scope + counsel-row decision.** yourco's **first gambling-adjacent prospect** — the 08-17 CRM note flags that nothing in `decisions/`, `processes/` or `counsel-gates.md` covers it. Not a build blocker yet, but a genuine pending-the Founder *decision*. | the Founder | 2026-08-17 | 4d | Decide: is (b) picks-generation in scope at all, and does this need a `counsel-gates.md` row like Care / Conduit? Conversation first; scope in the audit. |

## New today
- **No new queue item.** No deal entered its due window, no new the Founder-send draft appeared (inbox-triage created 0 drafts today), no watchdog/eval-review artifact refreshed (latest still 08-16 / 07-12).
- **Sample Client flipped 🟡→🔴 today** (8d past its next action) — the predicted age-flip from yesterday, a state change, not a new item. It now leads the revenue block (#2).
- **Noted, not nagged — inbox-triage's 08-21 fading carries.** (a) **Security confirm — two "new sign-in on Mac OS" alerts (08-18)**, labeled `Jim/Needs-the Founder`, line up with the Founder standing up the paid GCP account that day → almost certainly him; a one-glance close. Owned + surfaced daily by inbox-triage, so tracked here, **not double-nagged** onto the formal queue. (b) **Sample Client render budget FYI** — $12.50 of the $25/mo Gemini cap, metering as intended; healthy, no decision. (c) ForesightIQ — already queued (#14).
- **Noted, not nagged — three future-dated follow-up tasks.** `t_fl2v8` (Partner C), `t_qc96n` (Sample Contact), `t_r94ln` (Sample Contact) — all `due 2026-09-15`, `done:false`, owner the Founder. **Future-dated → not overdue → not a nag.** Surface the moment 09-15 passes without action.
- **Still tracked, not nagged — Sample Contact partner-track vs. full cap table (carried).** `c7s6si` stays connector→"prospective PARTNER (the Founder 2026-08-18)"; the 50/35/15 split is fully allocated and unsigned, and the 08-16 meeting substance is still uncaptured. A facet of the OA decision already queued as **#3**, not a separate nag.
- **Still open, not queued — referral-attribution capture gap (carried).** The 08-17 CRM notes on Sample Contact → Sample Contact (c31/d31) and Sample Contact → Sample Company A/Sample Company A (c8/d8) remain **UNCONFIRMED**; no `referredDate` is recorded on any, so **no §6b SLA is running** (correctly — an unconfirmed relation is not a referral). Under the one-rate-card these carry a commission if real, so confirming them is worth a capture pass. Owner: CRM capture (David) + the Founder to confirm. Tracked, not nagged.

## Reconciliation flags
- **`t_hank`** — `done:true` while c18/p15/d18 all blank/past-due. **9th consecutive run.** c18 + p15 re-verified blank directly this run; flag unchanged (CRM `meta.updated` static at 08-19). Kept queued (#9).
- **`t_warmup`** (`done:false`, due 06-20) — functionally satisfied (Instantly `avgScore 100` ≥90% bar) since 08-07; **not** queued. Stale in the trailing-true direction; noted, not nagged.
- **`t_sc_start`** — `done:false`, `due 2026-10-01`, explicitly PAUSED pending the Sample Client OS re-scope. Not overdue; operational park, not a nag.
- **`meta.updated` static at 08-19 (no move since yesterday)** — no cleared/changed item; the four in-motion deal fields re-checked and unchanged (per `checkbox-is-not-clearing-evidence` + `cross-session-drift`: the timestamp isn't evidence; the fields are, and they didn't move).
- **Briefing / social drafts not queued** — the 08-17 Monday-Briefing draft (`1a00f959`) is the briefing loop's own self-addressed output; the 06-14 LinkedIn "draft for approval" (`19ec7bcb`) and the 06-09 Reilly Landscaping-batch draft (`19eacea0`) are OtherVenture-parked staged outbound; none is a the Founder-send action-item, consistent with every prior run.
- **No item vanished without evidence.** All 15 prior items reconciled: 0 cleared, 15 carried (+1), 0 genuinely new.

## Escalations
- **Warm-network (#1, 69d) is now on its FIFTH consecutive no-movement run** (08-17 · 08-18 · 08-19 · 08-20 · 08-21) — well past the SOP's 2-run watchdog trigger. It **leads the Slack post in bold**. By the >21d rule it is an unmade decision, not a queue item: act (send 3 texts) or formally delegate to Reilly.
- **Sample Client (#2) crossed into 🔴 today** — the OS's single closest-to-revenue deal ($12k) is now 8d past its next action and >7-day red. Flagged yesterday as tomorrow's flip; it happened on schedule, so it isn't read as sudden. Two-run 🔴-no-movement watchdog trigger arms Monday if it doesn't move over the weekend.
- **The shape is unchanged:** solvency (#4, $0 cash) and liveness (#6) are the existential backdrop; warm-network is the oldest revenue gap; one counsel engagement (#3) still moves fourteen gates but is gated behind the Founder's own D10–D12 answers — with the Lucas partner-track wrinkle riding along.
- **Nine items are >21 days** (#7 71d · #1 69d · #8 66d · #3/#6 64d · #9 63d · #11 54d · #5 52d · #12 51d). Each is really an unmade decision — the recommended verb is *act, delegate, or formally park*, not "carry again."
- **Nothing moved in ~24h** — expected for a quiet Fri tick; flagged plainly (not as failure) so the flat board isn't misread as the loop missing something.

## Parked by decision (inventory — NOT nagged)
Friday run → **not surfaced to Slack** (parked inventory is Monday-only, SOP §5). **Unchanged from the 2026-08-17 Monday inventory** (14 gates, verified there against `counsel-gates.md` update-log through 08-13 + `launch-gate.md`): referral MLM override, rep equity, yourco Care, Conduit, Sample Product public, ownership promise, SC scaled postcard, trip-wire pricing language, the OtherVenture launch gate, self-serve SaaS, per-vertical funnel. Next full surface: **Mon 2026-08-24.** No parked item changed state; none is silently rotting.

## Referred-intro SLA (SOP §6b) — clean
Swept all companies in `crm/data.json` for `referredDate`: **none carry one**; every `referrer`/`referredByCompany` field is empty. The 08-17 notes discuss *potential* attribution (Chris→Tom, Reid→Sample Company A) but explicitly record it as UNCONFIRMED and **not** as referrals — so no first-touch SLA is running. **Nothing to lead the post with on this axis** (n=0 referrals; the connector engine is pre-launch).

## What I'd do differently next run
(Empty — for the Founder to fill)
