2026-08-21 — A trip-wire with prose in its `Check:` slot reports `error`, fakes protection, and nobody owns fixing it — the fix is the documented `_none` form

Source: Melanie initiative loop 2026-08-21, acting on `loops/_consistency/2026-08-18.md` ("2 trip-wire check(s) cannot be evaluated ... unparseable term"). Two 08-17 decisions (exit-radar-platform, succession-three-play-map) had rich revisit prose sitting in the `**Check:**` slot instead of a `<fact> <op> <number>` term, so `dashboard/tripwires.py` reported them as `error` — and per `decisions/_TRIPWIRES.md`, an errored check is never read as "did not fire," so the decision looked protected while protecting nothing.

Pattern: The consistency watchdog *reports* this drift but does not fix it, and it isn't a human-action item Jim's queue would carry — so it rots silently between the two loops. The fix is mechanical and intent-preserving, not a judgment call: (1) the author's revisit prose misfiled in `Check:` IS the required `**Overturn if:**` line — relabel it, don't invent one; (2) set `**Check:** _none — <why it isn't an instrumented fact>_` (the grammar treats `_none` as prose, clearing the parse error); (3) keep the `**Review:**` date untouched — that is what still surfaces the decision as `due`. Do NOT substitute a partial machine check (e.g. `liveClients >= 3`) for a multi-condition prose gate — that recreates the exact "turns a nuanced revisit trigger into a green light" failure `_TRIPWIRES.md` warns about; `_none` is the honest state when the real gate isn't instrumented.

Caveat (headless): the governing verification — run `python3 dashboard/tripwires.py` and confirm the decision now reads `watching`/`due`, not `error` (`learnings/ops/2026-08-13_tripwires-need-a-time-fence.md`) — needs Bash, which is gate-denied on the runtime (`learnings/ops/2026-08-20_headless-gate-blocks-in-session-instrumentation.md`). The `_none` conversion cannot fire wrong (nothing to fire), so the edit is safe unverified; the next consistency run (Mon) is the confirmation.

Audience: anyone fixing a `_consistency` tripwire-drift item; Ray + Rafi (gate trackers carry checks); Atlas (HQ surfaces the verdict); Kolby (eval).
Triggers: tripwire error, unparseable term, consistency drift, path:decisions/, writing a trip-wire, overturn condition, agent:ray, agent:rafi
