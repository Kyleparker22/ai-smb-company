# _network/candidates — the review inbox

Anonymized candidates copied here from tenant `pattern-candidates/` queues, tenant tag replaced by an opaque engagement ID. This copy is the ONLY path across a tenant boundary. Nothing publishes from here without the full human gate (Kolby → Rafi → the Founder) and a `REVIEW-LOG.md` row — see `runtime/immune/README.md`. Empty pre-clients is expected.
