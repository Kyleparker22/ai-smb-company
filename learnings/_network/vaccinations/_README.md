# _network/vaccinations — the published feed of record

Human-approved network patterns, `YYYY-MM-DD_slug.md`, candidate schema + severity + "what every OS should change." Every file here must trace to an `approved` row in `../REVIEW-LOG.md` (consistency-check provenance invariant). Client OSes absorb these via their `vaccinations/` Step-0 feed and log absorption in their `_applied.md`. Gate doc: `runtime/immune/README.md`.
