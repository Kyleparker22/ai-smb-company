# Katie — Stage 2: Build

## Build approach
Handoff build, like Charles. The content loop SOP and prior artifacts already exist under Atlas; building Katie means giving the loop a named owner, holding it to a content-specific eval (voice, moat-anchoring, distinctiveness), and keeping the publish gate firmly with the Founder. Low risk — read + draft only; nothing publishes without approval.

## Components
### 1. Content cadence loop
`processes/loops/content.md` — Friday AM. Now runs as Katie. Picks a theme, drafts LinkedIn/X/newsletter in the Founder's voice, generates follow-up angles, writes `loops/content/YYYY-MM-DD.md`, posts an internal notice.

### 2. Style guide (in the SOP)
The rigorous voice rules already in `content.md`: concise/direct, outcomes not features, sell the moat not the tooling, real examples > abstractions, no buzzword salad, no emoji unless organic, reject anything generic.

### 3. Hook intake
Reads the latest `loops/sales/` artifact so a real prospect objection or pattern becomes a public post — content grounded in actual market signal.

## Inherited vs new
- **Inherited from Atlas:** the content loop SOP + prior artifacts.
- **New for Katie:** named ownership + signature, the eval set (`03_eval.md`), the boundary with Reed (video) and Pickle (collateral), and the explicit publish-approval gate.

## Patterns reused / contributed
- **Reuses:** loop SOP convention, closed-loop feedback section, Slack notice, the style-guide-as-eval pattern.
- **Contributes to `yourco-template`:** a reusable **content/thought-leadership module** + the **voice-and-distinctiveness eval** (a content-quality gate any client-facing content employee could use).

## Build status
- [x] Content loop SOP exists (`processes/loops/content.md`)
- [x] Engagement docs scaffolded (this folder)
- [x] SOP ownership handed to Katie (header + signature updated)
- [x] Roster + pipeline updated (Katie → live/in-build)
- [ ] Content scheduled task pointed at Katie's identity/SOP (manual; currently signs as Atlas; non-blocking)
- [ ] `contact@yourco.example.com` alias created (non-blocking)
- [ ] First content brief produced *as Katie* and judged on-voice + distinct by the Founder

## Autonomy
Governed by the standard in `processes/autonomy-matrix.md` (rungs R0–R3; default trajectory = full autonomy, earned per-action on Kolby's eval evidence; unproven/irreversible actions start gated at R1). Katie's actions mapped to rungs:

| Action | Rung | Notes |
|---|---|---|
| Read sales/content artifacts, pick theme, research | **R3** | inherently safe |
| Draft LinkedIn/X/newsletter copy, write `loops/content/` artifact, post internal `#yourco-katie`/`#all-yourco` notice | **R3** | internal, reversible in git |
| **Post externally to social** (LinkedIn/X) | **R1 → R2** | starts gated (the Founder commits the post); **climbs to R2** (auto + notify + reversible window) on Kolby's clean on-voice/distinctiveness/honesty eval record, never to unattended R3 by default — Katie is sender-of-record for yourco's public voice |
| Any claim about results/clients/metrics | **R1 (hard floor)** | overclaim watchdog; pre-revenue = none unless real + approved — stays gated regardless of evidence |

**Hard-floor / gated:** external social posting starts at R1 and advances only on eval evidence (ceiling R2); fabricated/unverified result claims stay R1 by design.

## Known overlay decisions
- **v0 runs under the Founder's identity**; internal notices signed "— Katie, YourCo Ops"; public content publishes as the Founder/YourCo.
- **Atlas stays the reader, not the owner** — Monday briefing reads content output; no orchestration (siblings, the Founder conducts).
- **Handoff logged** in `decisions/2026-06-07_katie-content-agent.md`.
