# OS-framing alignment + email change (2026-06-23)

> **Owner: Webb.** Three the Founder requests. Staged/internal.

## 1. Email — sitewide
`hello@yourco.example.com` → **`founder@yourco.example.com`** across **20 pages** (footers + any inline mailto). 0 `hello@` remain. (Other addresses like `privacy@` left as-is.)

## 2. "48 hours, documented" (timeline-48h.html) — AI OS framing
Reframed the 48-hour build as **the first capability of your AI OS** (not a standalone employee): hero lede + ctx now say "the first capability of your AI OS … the rest follow, one at a time"; the go-live milestone → "the first capability of your AI OS, live … the next capability follows"; endcard → "the AI OS that keeps growing on top of it"; meta updated. (Names/verticals were already aligned earlier.)

## 3. Reviewed + aligned 5 pages to OS-first / agent terminology
Minimal copy-only edits (no CSS/structure/nav/footer changes); no fabricated metrics.
- **about.html** (heaviest reframe, mine): "boutique AI-employee implementation business" → "boutique AI implementation business"; lead reframed to *audit → custom AI OS (a coordinated team of agents)*; "Employees, not tools." → "Agents, not tools." (+ "start with one; the full OS is where the return is"); "a working employee" → "a working system / agents"; "We run yourco on the employees we build" → "…on the agents we build"; meta updated.
- **objections.html** (Honest answers): meta "AI employees" → "a custom AI OS"; "The employee runs in your tenant" → "Your AI OS runs in your tenant"; "managed employee" → "managed AI OS".
- **compare.html**: yourco column "A managed employee" → "A managed AI OS" (kept "An employee" in the human-hire comparison column — correct).
- **reliability.html**: lede "making it safe to run" → "making a whole AI OS safe to run"; closer "an yourco employee can run" → "an yourco AI OS can run".
- **eval-gated-seal.html**: all 5 "employee" → "agent" (the seal certifies an individual agent — "AI OS" not forced here).

## Verification
All 6 pages: HTTP 200, `the Founder@` present, `hello@` gone, OS/agent framing in place; about renders clean, 0 console errors. (Note: the linked-`site.css` pages may show the headline in system-sans in the preview's stale-CSS cache — a known preview artifact; the serif loads on a real/hard-refresh.)
